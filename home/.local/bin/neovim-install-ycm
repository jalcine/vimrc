#!/usr/bin/env sh
# vim: set fdm=marker :

cd "${HOME}/.config/nvim"

pyenv rehash

${HOME}/.config/nvim/plugins/YouCompleteMe/install.py \
  --tern-completer \
  --clang-completer

cargo install racer

npm install -g typescript
